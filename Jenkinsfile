pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the app in github'
            }
        }
        stage('test') {
            steps {
                echo 'testing the app in github'
            }
        }
        stage('Deploy') {
            steps {
                echo 'deploying the app in github'
            }
        }
    }
}
